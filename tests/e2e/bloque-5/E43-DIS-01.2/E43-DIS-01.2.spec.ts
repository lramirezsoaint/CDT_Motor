import { test } from '@fixtures/base.fixture';

test('@bloque5 @E43-DIS-01.2 @am @distribucion @admin @critical elimina distribucion con errores', async () => {
  test.skip(
    true,
    'TODO [DATA]: el catalogo no define una distribucion especifica en estado Con Errores segura para eliminar.',
  );
});
