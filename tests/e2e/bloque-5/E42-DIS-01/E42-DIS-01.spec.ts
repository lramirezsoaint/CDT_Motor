import { test } from '@fixtures/base.fixture';

test('@bloque5 @E42-DIS-01 @am @distribucion @gestor-asiento @regression copia distribucion completada', async () => {
  test.skip(
    true,
    'TODO [DATA]: el catalogo no define la distribucion completada origen ni periodo/version destino para confirmar la copia sin inventar datos.',
  );
});
