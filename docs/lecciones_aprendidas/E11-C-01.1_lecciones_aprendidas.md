# E11-C-01.1 lecciones aprendidas

- La validación correcta del caso no es mirar cabeceras en la UI, sino contrastar el Excel descargado contra toda la información visible de la tabla.
- Para `Líneas`, el caso ahora compara todas las filas visibles del frontend contra el workbook exportado por columnas de negocio.
- La UI muestra `Modificado`, pero el Excel exportado actual no trae esa columna; con la validación fuerte esto queda expuesto como bug funcional.
- Este caso quedó movido a su carpeta exacta para evitar trazabilidad duplicada en `sin-trazabilidad`.
